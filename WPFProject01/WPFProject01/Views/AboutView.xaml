<UserControl
    x:Class="WPFProject01.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WPFProject01.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPFProject01.Views"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mf="http://namespaces.mindscape.co.nz/wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:ConnectionIndicator
            x:Name="StatusIndicator"
            Grid.Row="0"
            Width="40"
            Height="40"
            State="Idle" />

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button Click="OnConnect" Content="连接" />
            <Button Click="OnDisconnect" Content="断开" />
        </StackPanel>


        <StackPanel
            Grid.Row="2"
            VerticalAlignment="Bottom"
            Orientation="Horizontal">
            <TextBlock Text="当前版本为：" />
            <TextBlock Text="{Binding Version}" />
        </StackPanel>
    </Grid>
</UserControl>