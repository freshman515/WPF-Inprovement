<UserControl x:Class="WPFProject01.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helper="clr-namespace:WPFProject01.Helper"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:WPFProject01.ViewModels"
             d:DataContext="{d:DesignInstance vm:LoginViewModel}"
             xmlns:local="clr-namespace:WPFProject01.Views"
             FontFamily="Microsoft YaHei"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*" />
            <RowDefinition Height="1.5*" />
            <RowDefinition Height="1.5*" />
        </Grid.RowDefinitions>
        <Border>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Offset="0.0" Color="#383B43" />
                    <GradientStop Offset="0.5" Color="#BAC7E3" />
                    <GradientStop Offset="1.0" Color="#ffffff" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <md:PackIcon Kind="ChartScatterPlot" Width="50" Height="50" VerticalAlignment="Center"
                         HorizontalAlignment="Center">
            </md:PackIcon>
            <TextBlock Text="登录界面" FontSize="40" FontFamily="Microsoft YaHei" VerticalAlignment="Center"></TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1" Background="White" Width="240">
            <TextBox Width="240" Height="50" 
                     FontSize="12"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,5,0,5"
                     md:TextFieldAssist.TextFieldCornerRadius="10"
                     md:HintAssist.Hint="输入账号"
                     md:HintAssist.IsFloating="True"
                     md:TextFieldAssist.PrefixText="👤"
                     Text="{Binding Username, Mode=TwoWay}" />
            <PasswordBox Margin="0,5,0,5" 
                         md:HintAssist.Hint="输入密码"
                         md:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         md:TextFieldAssist.PrefixText="🔒"
                         FontSize="12"
                         md:TextFieldAssist.TextFieldCornerRadius="10"
                         Height="50" 
                         helper:PasswordBoxHelper.Attach="True"
                         helper:PasswordBoxHelper.Password="{Binding Password,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                         Width="240">
                <PasswordBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding LoginCommand}"></KeyBinding>
                </PasswordBox.InputBindings>
            </PasswordBox>
            <Button Width="100" Height="auto" Background="PaleTurquoise" 
                    md:ButtonAssist.CornerRadius="8"
                    Foreground="Black" Content="注册账号" HorizontalAlignment="Right"
                    Command="{Binding RegisterCommand}"/>
        </StackPanel>
        <Button VerticalAlignment="Center" Grid.Row="2" Width="200"
                Content="登录"
                Command="{Binding LoginCommand}"
                Background="PaleTurquoise"
                Foreground="Black"
                md:ButtonAssist.CornerRadius="15"
                md:ButtonProgressAssist.IsIndeterminate="True"
                md:ButtonProgressAssist.IsIndicatorVisible="True"
                md:ButtonProgressAssist.Value="-1"
                Style="{StaticResource MaterialDesignRaisedButton}">
        </Button>
    </Grid>

</UserControl>