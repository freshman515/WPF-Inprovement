<UserControl x:Class="WPFProject01.Views.UserOperatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFProject01.Views"
             xmlns:vm="clr-namespace:WPFProject01.ViewModels"
             d:DataContext="{d:DesignInstance vm:UpdateViewModel}"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helper="clr-namespace:WPFProject01.Helper"
             mc:Ignorable="d"
             Height="250" Width="400">
    <Border Background="Azure">
        <StackPanel HorizontalAlignment="Center" Width="200">
            <TextBlock Text="用户信息" FontSize="20" HorizontalAlignment="Center" Margin="0,5" />
            <TextBox md:HintAssist.Hint="用户名" Text="{Binding User.Username}" Width="200"
                     HorizontalAlignment="Center"
                     Margin="0,20,0,5" />
            <TextBox md:HintAssist.Hint="密码" Text="{Binding User.Password}" Width="200"
                     HorizontalAlignment="Center"
                     Margin="0,5" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,5">
                <TextBlock VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Gray">选择角色</TextBlock>
                <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding User.Role}"></ComboBox>
            </StackPanel>
            <TextBox md:HintAssist.Hint="手机号" Text="{Binding User.Telephone}" Width="200" Margin="0,5"
                     HorizontalAlignment="Center" />
            <DockPanel LastChildFill="False" Margin="0,10">
                <Button Background="Coral"
                        Command="{Binding ConfirmCommand}"
                        Width="80"
                        DockPanel.Dock="Left"
                        BorderBrush="Aqua"
                        Content="确定" />
                <Button Background="gray" 
                        Width="80"
                        BorderBrush="Aqua"
                        Command="{Binding CancelCommand}"
                        Content="取消" 
                        DockPanel.Dock="Right" />
            </DockPanel>
        </StackPanel>
    </Border>

</UserControl>