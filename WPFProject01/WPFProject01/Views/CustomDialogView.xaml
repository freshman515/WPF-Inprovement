<UserControl x:Class="WPFProject01.Views.CustomDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:vm="clr-namespace:WPFProject01.ViewModels"
             d:DataContext="{d:DesignInstance vm:CustomDialogViewModel}"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFProject01.Views"
             mc:Ignorable="d">
    <StackPanel Width="300" Margin="20">
        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />
        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="14" />
        <DockPanel LastChildFill="False">
            <DockPanel.Style>
                <Style TargetType="DockPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Buttons}" Value="YesNo">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Buttons}" Value="Ok">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DockPanel.Style>
            <Button Content="确定" Margin="0,20,0,0"
                    Background="DodgerBlue"
                    HorizontalAlignment="Center" Width="80"
                    Command="{Binding ConfirmCommand}" />
        </DockPanel>
        <DockPanel LastChildFill="False">
            <DockPanel.Style>
                <Style TargetType="DockPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Buttons}" Value="YesNo">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Buttons}" Value="Ok">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DockPanel.Style>

            <Button Content="取消" Margin="0,20,0,0"
                    DockPanel.Dock="Left" Width="80"
                    Background="DarkGray"
                    BorderBrush="Fuchsia"
                    Command="{Binding CloseCommand}" />
            <Button Content="确定" Margin="0,20,0,0"
                    Background="CadetBlue"
                    DockPanel.Dock="Right" Width="80"
                    Command="{Binding ConfirmCommand}" />
        </DockPanel>
    </StackPanel>
</UserControl>