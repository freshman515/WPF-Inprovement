<UserControl x:Class="WPFProject01.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFProject01.Views"
             xmlns:vm="clr-namespace:WPFProject01.ViewModels"
             d:DataContext="{d:DesignInstance vm:HomeViewModel}"
             xmlns:controls="clr-namespace:WPFProject01.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    <StackPanel>
        <controls:UserCard UserName="{Binding UserSession.User.Username}"
                           Title="WPF开发"
                           Email="2200454726@qq.com"
                           Avatar="pack://application:,,,/Assets/cap.jpg"
                           ImgHeight="100"
                           ImgWidth="100" />
        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding RefreshCommand}" Width="100" Background="Indigo" HorizontalAlignment="Left">刷新</Button>
            <Border BorderBrush="Black" BorderThickness="0" Margin="20,0" Background="Ivory">
                <StackPanel Orientation="Horizontal">
                    <TextBox Text="{Binding City,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="10,0" VerticalAlignment="Center" FontSize="16">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding GetCityWeatherCommand}"></KeyBinding>
                        </TextBox.InputBindings>    
                    </TextBox>
                    <TextBlock Text="天气：" VerticalAlignment="Center" FontSize="16"></TextBlock>
                    <TextBlock Text="{Binding Weather.Current.TempC}" VerticalAlignment="Center" FontSize="16"></TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Products}" AutoGenerateColumns="False" HeadersVisibility="Column"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" IsReadOnly="True" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Name" IsReadOnly="False" Binding="{Binding Name,Mode=TwoWay}" Width="100" />
                <DataGridTextColumn Header="Description" IsReadOnly="False" Binding="{Binding Description,Mode=TwoWay}"
                                    Width="200" />
                <DataGridTextColumn Header="Price" IsReadOnly="False"
                                    Binding="{Binding Price,Mode=TwoWay, StringFormat={}{0:F2}}" Width="80" />
                <DataGridTextColumn Header="Stock" IsReadOnly="False" Binding="{Binding Stock,Mode=TwoWay}" Width="80" />
                <DataGridTextColumn Header="Category" IsReadOnly="False" Binding="{Binding CategoryName}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

    </StackPanel>

</UserControl>