<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPFProject01.Controls">
    <Style TargetType="local:ConnectionIndicator">
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ConnectionIndicator}">
                    <Grid>
                        <Ellipse
                            x:Name="StatusCircle"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            Fill="Gray" />
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="ConnectionStates">
                                <VisualState x:Name="Connected">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="StatusCircle"
                                            Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                                            To="Green"
                                            Duration="0:0:0.3" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disconnected">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="StatusCircle"
                                            Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                                            To="Red"
                                            Duration="0:0:0.3" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Idle">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="StatusCircle"
                                            Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                                            To="Gray"
                                            Duration="0:0:0.3" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>