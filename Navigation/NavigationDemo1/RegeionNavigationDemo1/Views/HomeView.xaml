<UserControl x:Class="RegeionNavigationDemo1.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:RegeionNavigationDemo1.ViewModels"
             xmlns:viewModels="clr-namespace:RegeionNavigationDemo1.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:HomeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
       <StackPanel >
           <TextBlock Text="HomeView" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center" Background="White"></TextBlock>
            <TextBlock Text="Id:"/>
            <TextBlock Text="{Binding Args.UserId}"></TextBlock>
            <TextBlock Text="Username:"/>
            <TextBlock Text="{Binding Args.UserName}"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
